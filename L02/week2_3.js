const a = 5;
const b = 10;

const add = function(x, y) {
    return x + y;
};
console.log(`the sum of ${a} and ${b} is ${add(a, b)}`); // Output: the sum of 5 and 10 is 15


const aood =(x, y) => x + y;
console.log(aood(a, b)); // Output: 15




const subtract = (x, y) => {
    const result = x - y;
    return result;
}
console.log(subtract(b, a)); // Output: 5